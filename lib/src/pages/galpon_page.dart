import 'package:flutter/material.dart';

class GalponPage extends StatelessWidget {
  const GalponPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Galpon'),);
  }
}
