import 'package:flutter/material.dart';

class RecursosPage extends StatelessWidget {
  const RecursosPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Recursos'),);
  }
}
